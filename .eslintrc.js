module.exports = {
    root: true,
    parserOptions: {
        // or 'eslint' if not using babel
        parser: 'babel-eslint'
    },
    env: {
        browser: true
    },
    extends: ['airbnb']
}